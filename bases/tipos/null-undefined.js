"use strict";
(() => {
    //tsconfig strictNullChecks, si se desactiva esta opcion el tipo
    //boolean puede tomar estos 4 valores 
    //    let isActive: boolean = null; 
    let isActive = true;
    //    el boolean puede tener estos valores true, false, undefined, null
    //console.log(isActive);
});
